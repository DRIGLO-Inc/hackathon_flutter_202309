import 'package:flutter/material.dart';

class MatchRoomBeforeStartView extends StatelessWidget {
  const MatchRoomBeforeStartView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
